<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="za.ac.uct.cs.powerqope">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".adapter.VideoAdapter"></activity>
        <activity android:name=".utils.Video" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".PrivateDNSActivity" />
        <activity android:name=".VPNSettingsActivity" />
        <activity android:name=".AdvancedActivity"
                  android:alwaysRetainTaskState="true"

                    />
        <activity android:name=".activity.SpeedTestActivity" />
        <activity android:name=".PlayerActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>